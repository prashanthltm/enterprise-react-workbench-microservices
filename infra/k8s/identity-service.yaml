apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
        - name: identity-service
          image: <acr-name>.azurecr.io/identityservice:latest
          ports:
            - containerPort: 8080
          env:
            - name: KeyVault__VaultUri
              value: "https://<keyvault-name>.vault.azure.net/"
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
spec:
  selector:
    app: identity-service
  ports:
    - port: 5001
      targetPort: 8080
  type: ClusterIP
